# Fun > 1. Type Properties

You can look up a property from a type in the type system using `[]` index syntax.
This `index.d.ts` looks up the `Emoji` property in the `ComponentRegistry` type, which is `"✨"`.
